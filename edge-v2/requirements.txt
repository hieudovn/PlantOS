# PlantOS Edge v2 — Python dependencies
# Reuses DuckDB buffer, sync, health, publisher from Edge v1 (edge/agent/)

# Core
pyyaml>=6.0
duckdb>=0.10.0
httpx>=0.27.0

# MQTT
paho-mqtt>=1.6.1

# Web server (same as Edge v1)
aiohttp>=3.9.0

# Auth
bcrypt>=4.1.0
itsdangerous>=2.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
